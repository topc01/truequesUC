<!DOCTYPE html>
<html>
<head>
    <title>TruequesUC - Intercambia tus muebles y productos de casa</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
    <style>
        /* Agrega un estilo personalizado para el fondo degradado con imagen */
        .hero {
            background-image: linear-gradient(rgba(255, 255, 255, 0.8), rgba(255, 255, 255, 0.3)), url('/assets/salon-con-diferentes-muebles-para-guardar-00560105_aa92107d_230503144421_1999x1333.jpg'); /* Ruta de tu imagen */
            background-size: cover;
            background-position: center;
        }
        /* Ajusta el color del texto para que sea visible en el fondo degradado */
        .hero-body h1, .hero-body h2 {
            color: #333333; /* Color de texto blanco */
        }
        .profile-image {
            max-width: 300px;
            max-height: 300px;
            width: auto;
            height: auto;
        }
        footer {
            padding: 20px;
            text-align: center;
            bottom: 0;
            width: 100%
        }
    </style>
</head>
<body>
    <section class="hero is-bold">
        <div class="hero-body">
            <div class="container" style="display: flex; justify-content: space-between; align-items: center;">
                <h1 class="title is-size-1">
                    TruequesUC
                </h1>
                <h2 class="subtitle">
                    Intercambia tus muebles y productos de casa
                </h2>
            </div>
        </div>
    </section>

    <section class="section">
        <div class="container">
            <h1 class="title">Editar Perfil</h1>
        </div>
    </section>

    <section class="section">
        <div class="columns is-centered">
            <div class="column is-6"> <!-- Cambiamos el ancho de la columna -->
                <div class="card">
                    <div class="card-content">
                        <div class="content">
                        <div class="field has-text-centered">
                        <% if @client.image %>
                            <%= image_tag @client.image_url, alt: 'Imagen de perfil', class: "profile-image" %>
                        <% else %>
                            <%= image_tag 'perfil.png', alt: 'Imagen de perfil', class: "profile-image" %>
                        <% end %>
                    </div>
                            <%# <div class="field has-text-centered"> <!-- Alineamos el campo al centro -->
                                <label class="label">Subir archivo</label>
                                <button type="button" class="button is-info">Subir</button>
                            </div> %>
                            <%= form_for(@client, url: update_profile_path, method: :patch) do |f| %>
                                <div class="field">
                                    <%= f.label :image, "Subir imagen:" %>
                                    <div class="control">
                                        <%= f.file_field :image, class: "input" %>
                                    </div>
                                </div>
                                <div class="field">
                                    <%= f.label :name, "Nombre:" %>
                                    <div class="control">
                                        <%= f.text_field :name, class: "input" %>
                                    </div>
                                </div>
                                <div class="field">
                                    <%= f.label :username, "Nombre de Usuario:" %>
                                    <div class="control">
                                        <%= f.text_field :username, class: "input" %>
                                    </div>
                                </div>
                                <div class="field">
                                    <%= f.label :phone_number, "Número de teléfono:" %>
                                    <div class="control">
                                        <%= f.text_field :phone_number, class: "input" %>
                                    </div>
                                </div>
                                <div class="field">
                                    <%= f.label :description, "Descripción:" %>
                                    <div class="control">
                                        <%= f.text_field :description, class: "input" %>
                                    </div>
                                </div>
                                <div class="field is-grouped is-justify-content-space-between">
                                    <div class="control">
                                        <a href="<%= client_show_path(@client.id) %>" class="button is-info">Volver</a>
                                    </div>
                                    <div class="control">
                                        <%= f.submit "Guardar cambios", class: "button is-primary" %>
                                    </div>
                                </div>
                            <% end %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</body>
<footer>
<p>© 2023 TruequesUC. Todos los derechos reservados.</p>
</footer>

</html>
