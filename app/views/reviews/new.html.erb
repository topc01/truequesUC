<!DOCTYPE html>
<html>
<head>
    <title>TruequesUC - Intercambia tus muebles y productos de casa</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
    <style>
        /* Agrega un estilo personalizado para el fondo degradado con imagen */
        .hero {
            background-image: linear-gradient(rgba(255, 255, 255, 0.8), rgba(255, 255, 255, 0.3)), url('/assets/salon-con-diferentes-muebles-para-guardar-00560105_aa92107d_230503144421_1999x1333.jpg'); /* Ruta de tu imagen */
            background-size: cover;
            background-position: center;
        }
        /* Ajusta el color del texto para que sea visible en el fondo degradado */
        .hero-body h1, .hero-body h2 {
            color: #333333; /* Color de texto blanco */
        }
        footer {
            padding: 20px;
            text-align: center;
            bottom: 0;
            width: 100%
        }
    </style>
</head>
<body>
    <section class="hero is-bold">
        <div class="hero-body">
            <div class="container" style="display: flex; justify-content: space-between; align-items: center;">
                <h1 class="title is-size-1">
                    TruequesUC
                </h1>
                <h2 class="subtitle">
                    Intercambia tus muebles y productos de casa
                </h2>
            </div>
        </div>
    </section>

    <section class="section">

        <div class="container">
            <h1 class="title">Escribir una reseña para <%= @visited_client.username %></h1>
        </div>

        <div class="container">
            <% url_ = "/clients/" + @visited_client.id.to_s + "/reviews" %>
            <%= form_for [@visited_client, @review], url: url_, html: { class: "my-custom-form-class" } do |f| %>
                
                <div class="field">
                    <%= f.label :title, "Título de la reseña", class: "label" %>
                    <div class="control">
                        <%= f.text_field :title, class: "input", placeholder: "Título de la reseña" %>
                    </div>
                </div>

                <div class="field">
                    <%= f.label :content, "Contenido de la reseña", class: "label" %>
                    <div class="control">
                        <%= f.text_area :content, class: "textarea", placeholder: "Escribe tu reseña aquí" %>
                    </div>
                </div>

                <div class="field">
                    <%= f.label :grade, "Calificación (entre 1 y 7)", class: "label" %>
                    <div class="control">
                        <%= f.number_field :grade, in: 1..7, class: "input" %>
                    </div>
                </div>

                <div class="field is-grouped is-justify-content-center">
                    <div class="control">
                        <%= f.submit "Enviar reseña", class: "button is-primary" %>
                    </div>
                </div>
            <% end %>
        </div>
    </section>
    <footer>
    <p>© 2023 TruequesUC. Todos los derechos reservados.</p>
</footer>

</body>
</html>
